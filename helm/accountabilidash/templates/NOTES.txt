Accountabilidash has been deployed!

1. PostgreSQL: {{ if .Values.postgres.useCloudNativePG }}CloudNative-PG cluster deployed. Backend connects to {{ .Values.postgres.cnpg.clusterName }}-rw.{{ else }}External connection. Ensure postgres.external.host and postgres.external.existingSecret are set.{{ end }}

2. Images: Using {{ .Values.global.imageRegistry }}{{ if .Values.global.imageOwner }}/{{ .Values.global.imageOwner }}{{ end }}/accountabilidash-backend (and -frontend).
   If using GHCR, ensure CI has built and pushed images.

3. Get the application URL:

{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}
{{- end }}
{{- else }}
   Run: kubectl get svc -n {{ .Release.Namespace }} -l "app.kubernetes.io/instance={{ .Release.Name }}" -o wide
   Use port-forward to access: kubectl port-forward svc/{{ include "accountabilidash.fullname" . }}-frontend 3000:3000
{{- end }}

4. For production, set a strong backend.secretKey. With CNPG, also set postgres.auth.password.
