{{- if .Values.postgres.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "accountabilidash.fullname" . }}-postgres
  labels:
    {{- include "accountabilidash.postgres.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    {{- include "accountabilidash.postgres.selectorLabels" . | nindent 4 }}
{{- end }}
