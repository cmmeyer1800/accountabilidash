apiVersion: v2
name: accountabilidash
description: Helm chart for Accountabilidash - a fullstack goal tracking application
type: application
version: 0.1.0
appVersion: "0.1.0"
keywords:
  - goals
  - accountability
  - fullstack
maintainers:
  - name: Accountabilidash Team
# CloudNative-PG operator is NOT a dependency - install it separately to avoid CRD
# annotation size limits with ArgoCD. When useCloudNativePG=true we only create
# the Cluster CR. See README for operator install instructions.
